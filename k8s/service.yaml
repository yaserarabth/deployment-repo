apiVersion: v1
kind: Service
metadata:
  name: mypod-svc
  labels:
    app: mypod
spec:
  type: LoadBalancer
  selector:
    app: mypod
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
